<div xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.w3.org/1999/xhtml ../../schema/fhir-xhtml.xsd" xmlns="http://www.w3.org/1999/xhtml">

<div>
<blockquote class="ballot-note" id="bn">
Request for ballot feedback: We would like the 'SHOULD' assertions above to become 'SHALL' assertions.
We are invoking our usual implementer outreach process to find out whether anyone else objects, but please file tickets for any rules you do not believe can be enforced that way.
</blockquote>

<blockquote class="ballot-note" data-augury-generated="true" id="bn1">
  <p><b>Changes since</b> the last ballot: SearchParameter widened how it names resource types - <code>SearchParameter.base</code> and <code>SearchParameter.target</code> are now <code>uri</code> rather than <code>code</code>, so a search parameter can be defined against a logical model or other canonical resource and not only a named resource type. Jurisdiction element(s) are no longer deprecated (they keep an explanatory note), and the definitions of <code>SearchParameter.type</code> and of the missing-value semantics for <code>multipleOr</code> / <code>multipleAnd</code> were clarified.</p>
  <p><b>Compatible substantive</b></p>
  <ul>
    <li>Type of <code>SearchParameter.base</code> changed from <code>code</code> to <code>uri</code>, allowing a logical model or other canonical resource to be named as the base rather than only a resource-type code [<a href="https://jira.hl7.org/browse/FHIR-51651">FHIR-51651</a>]</li>
    <li>Type of <code>SearchParameter.target</code> changed from <code>code</code> to <code>uri</code>, same rationale [<a href="https://jira.hl7.org/browse/FHIR-51651">FHIR-51651</a>]</li>
  </ul>
  <p><b>Non-substantive</b></p>
  <ul>
    <li>Jurisdiction element(s) are no longer deprecated but retain an explanatory note [<a href="https://jira.hl7.org/browse/FHIR-53766">FHIR-53766</a>]</li>
    <li>Clarified the handling of <code>SearchParameter.type</code> in response to the request that it &ldquo;has no escape valve&rdquo; - a clarifying note was applied rather than adding a new type option [<a href="https://jira.hl7.org/browse/FHIR-55190">FHIR-55190</a>]</li>
    <li>Clarified the meaning when <code>SearchParameter.multipleOr</code> and <code>SearchParameter.multipleAnd</code> are absent [<a href="https://jira.hl7.org/browse/FHIR-40328">FHIR-40328</a>]</li>
  </ul>
</blockquote>
</div>

<div>
<a name="scope"></a>
<h2>Scope and Usage</h2>
<p>
A <code>SearchParameter</code> resource specifies a search parameter
that may be used on the RESTful API to search or filter on a resource. 
The SearchParameter resource declares:
</p>
<ul>
 <li>how to typically refer to the search parameter from a client (<a href="searchparameter-definitions.html#SearchParameter.code">SearchParameter.code</a>)</li>
 <li>how the search parameter is to be understood by the server</li>
 <li>where in the source resource the parameter matches</li>
</ul>
</div> 

<div> 
<a name="bnr"></a>
<h2>Boundaries and Relationships</h2> 
<ul>
 <li>Search Parameters are referred to by <a href="capabilitystatement.html">CapabilityStatement</a> resources via the canonical URL for a search 
    parameter (<a href="capabilitystatement-definitions.html#CapabilityStatement.rest.resource.searchParam.definition">CapabilityStatement.rest.resource.searchParam.definition</a>)</li>
 <li>Search Parameters can appear in an <a href="implementationguide.html">Implementation Guide</a> to specify how resources can be found</li>
</ul>

</div> 

<div>
<a name="bnc"></a>
<h2>Background and Context</h2>
<p>
Implementers should be familiar with the 
background and concepts described in <a href="search.html">Search</a> on the RESTful API before working with this resource.
</p>
<p>
There is a <a href="searchparameter-registry.html">registry of all Search Parameters</a>.
</p>
</div>


</div>
