[StructureDefinition]
validation=element.base cannot appear if there is no base on the structure definition;  All element definitions must have unique ids; sliceName must be a proper token
; can't manufacture these ones correctly

[SearchParameter/example-extension]
roundtrip=Names differ at /SearchParameter: publisher/status
; because draft is injected for r2 -> r3
