swagger: 2.0

info:  title: US Core Requestor
  description: This profile defines the expected capabilities of the US Core Requestor actor when conforming to the US Core IG.
  version: null
  # contact:
    # end user to fill out
  license:
    name: null

# host: [user to provide]
# basePath: [user to provide]
schemes:
  - https
  # - http this would not normally be allowed (see doco)
consumes:
produces:
