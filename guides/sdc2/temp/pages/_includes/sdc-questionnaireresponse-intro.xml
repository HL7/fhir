<div xmlns="http://www.w3.org/1999/xhtml">
  <div>
      <a name="scope"> </a>
      <h3 class="self-link-parent">
         <span class="sectioncount">{{site.data.pages[page.path].label}}.1</span> Scope and Usage <a title="link to here" class="self-link" href="{{page.path}}#scope">
            <img src="assets/images/link.svg"
                 class="self-link"
                 width="20"
                 height="20"/>
         </a>
      </h3>
      <p>
      This profile sets expectations for use of the <a href="{{site.data.fhir.path}}questionnaireresponse.html">QuestionnaireResponse</a> resource within the 
      <a href="index.html">Structured Data Capture</a> implementation guide.  This includes
      identifying which core elements and extensions must be supported.
    </p>
      <p>
      For the purposes of this profile, <i>Supported</i> means that clients SHALL be capable of processing the element/extension and use the information to control the display
      and information capture associated with the <a href="{{site.data.fhir.path}}questionnaireresponse.html">QuestionnaireResponse</a> resource.  It means that servers SHALL be 
      capable of persisting those elements and returning them in response to requests.
    </p>
  </div>
  <div>
      <a name="boundaries"> </a>
      <h3 class="self-link-parent">
         <span class="sectioncount">{{site.data.pages[page.path].label}}.2</span> Boundaries and Relationships <a title="link to here"
            class="self-link"
            href="{{page.path}}#boundaries">
            <img src="assets/images/link.svg"
                 class="self-link"
                 width="20"
                 height="20"/>
         </a>
      </h3>
      <p>
      This profile is related to the <a href="sdc-questionnaire.html">SDC Questionnaire Profile</a> which defines the characteristic of a form "design".  This
      profile focuses on the constraints appropriate to capturing the "answer(s)" to a pre-defined form.
    </p>
  </div>
</div>
